{% extends 'base.html.twig' %}

{% block title %}Edit Events{% endblock %}

{% block body %}
    <h1 class="border borer-light text-center fw-bold fz-5 p-4 bg-dark text-light ">Edit Events</h1>

    {{ include('events/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-info my-2" href="{{ path('app_events_index') }}">back to list</a>

    {{ include('events/_delete_form.html.twig') }}
{% endblock %}
